dataset:
  geometry:
    # Enable depth only when evaluating layer 11
    use_depth: true
    use_normal: false

model:
  params:
    # Layers to extract from OpenCLIP ViT-B/16
    layers_to_hook: [11]
    primary_layer: 11
    head:
      feature_keys: [11]
      primary_key: 11
    output_dir_name: clip/11_depth

