dataset:
  geometry:
    # Enable both geometry channels when evaluating layer 2
    use_depth: false
    use_normal: true

model:
  params:
    # Layers to extract from OpenCLIP ViT-B/16
    layers_to_hook: [5]
    primary_layer: 5
    head:
      feature_keys: [5]
      primary_key: 5
    output_dir_name: clip/5_normal
