dataset:
  geometry:
    # Enable normal only when evaluating layer 8
    use_depth: false
    use_normal: true

model:
  params:
    # Layers to extract from OpenCLIP ViT-B/16
    layers_to_hook: [8]
    primary_layer: 8
    head:
      feature_keys: [8]
      primary_key: 8
    output_dir_name: clip/8_normal

