dataset:
  geometry:
    # Enable normal only when evaluating layer 11
    use_depth: false
    use_normal: true

model:
  params:
    # Layers to extract from OpenCLIP ViT-B/16
    layers_to_hook: [11]
    primary_layer: 11
    head:
      feature_keys: [11]
      primary_key: 11
    output_dir_name: clip/11_normal

